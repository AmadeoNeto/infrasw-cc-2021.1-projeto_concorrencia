import ui.*;

public class UpdatePlayer extends Thread{

    PlayerWindow window;

    public UpdatePlayer(PlayerWindow window){
        this.window = window;
    }

    @Override
    public void run() {

    }
}
